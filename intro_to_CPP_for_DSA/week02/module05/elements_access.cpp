#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin>>s;
    cout<<s.at(6)<<endl; // s.at() for what index element we want to see..
    cout<<s.back()<<endl; // s.back() for last indexes elements
    cout<<s.front()<<endl; // s.back() for first indexes elements
    
    return 0;
}