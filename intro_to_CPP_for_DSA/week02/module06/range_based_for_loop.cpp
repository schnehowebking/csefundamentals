#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin>>s;
    for (char c:s)
    {
       cout<<c<<endl;
    }

    // [Note: If need to print index then we need to use for loop before]
    
    
    return 0;
}