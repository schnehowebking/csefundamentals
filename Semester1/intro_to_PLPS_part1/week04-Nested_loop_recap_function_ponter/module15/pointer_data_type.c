#include<stdio.h>

int main()
{
    int x=100;
    int *ptr = &x;

    // pointer always grab the data type of original variable...

    double x2= 200;
    double *ptr2 = &x;

    // ptr is a pointer so its need only 4bytes for it.

    


    
    return 0;
}